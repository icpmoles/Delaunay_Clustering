name: building_environment

services:
  triangulation_build:
    build: .
    network_mode: host
    volumes:
      - ./:/project_source/:ro
      - ./bin:/project/common_build_dir/:rw
    command: ["/bin/bash","/project_source/docker/entrypoint.sh"]